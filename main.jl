using TSPLIB

function constructiveSolution(tsp, startingNode)
    remainingNodes = Set(0:tsp.dimension)
    cost = 0
    delete!(remainingNodes, startingNode)


end

push!(visitedNodes, initialNode)
tsp = readTSP("TSP-MetaHeuristics/data/xqf131.tsp")

